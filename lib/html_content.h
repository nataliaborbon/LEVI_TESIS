#ifndef HTML_CONTENT_H
#define HTML_CONTENT_H

/** * @brief Cadena de texto constante que contiene el código HTML, CSS y JS.
 * Incluye el algoritmo de Eye Tracking (Elipse + Morfología) y la 
 * captura del stream de video de la S3.
 */
extern const char* aplicacionHTML;

#endif // HTML_CONTENT_H